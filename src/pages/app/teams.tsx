import PageTemplate from "../template";

export default function Teams() {
  return (
    <PageTemplate title="Our Teams">
      <p>
        From juniors to seniors, every squad is built on teamwork, passion, and
        dedication to the game.
      </p>
    </PageTemplate>
  );
}
